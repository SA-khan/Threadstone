import React from "react";

const header = () => {
  return React.createElement(
    "div",
    {},
    React.createElement("h4", null, "Threadstone Header Goes Here")
  );
};

export default header;
